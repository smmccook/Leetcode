class Solution:
    def missingNumber(self, nums: List[int]) -> int:
        
        n = len(nums)
        return sum(range(1,n+1)) - sum(nums)
            
#         n = len(nums)
        
#         check = [0] * (n + 1)
        
#         for num in nums:
#             check[num] += 1
        
#         return check.index(0)
